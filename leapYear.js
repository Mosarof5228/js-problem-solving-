/* function leapYearCheckMachine(year) {
    if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
        return "This year is leapYear";
    }
    else {
        return "This year is not LeapYear!!!!!!!!!!"
    }
}

const Result = leapYearCheckMachine(1960);
console.log(Result); */

